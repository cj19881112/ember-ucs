
import { indexOfTableItem } from 'library/helpers/index-of-table-item';
import { module, test } from 'qunit';

module('Unit | Helper | index of table item');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexOfTableItem([42]);
  assert.ok(result);
});

