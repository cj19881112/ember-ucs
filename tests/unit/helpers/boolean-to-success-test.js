
import { booleanToSuccess } from 'library/helpers/boolean-to-success';
import { module, test } from 'qunit';

module('Unit | Helper | boolean to success');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = booleanToSuccess([42]);
  assert.ok(result);
});

